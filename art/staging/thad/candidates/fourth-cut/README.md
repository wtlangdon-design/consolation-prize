# Fourth cut — articulated-arm profile walk, REJECTED (Tyler, 2026-09-05)

Preserved, not adopted. `tools/rig/character.py --level-limbs` on `art/staging/thad/profile-stride-01.png` (the single authored stride still, op `thad-profile-walk-continuity`): legs levelled and swung by sine, arms extended to the shoulder and swung there, per-frame grounding. Eight frames, two-step cycle, `strideLength` 149. Deployed on main as 2006331 (PR #7). Tyler's rulings across the four cuts: "gallops" (cut 1), "weird dance move" (cut 2), "severed limbs" (cut 3), and on this cut: do not puppet-rig a single still. Superseded in the shipping tree by the authored whole-body cycle (`../authored-cycle`).

`thad-walk-right/` and `thad-walk-left/` are the exact directories that shipped; `thad.record.json` is the actor record that went with them.
