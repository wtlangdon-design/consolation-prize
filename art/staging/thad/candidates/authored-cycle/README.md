# Authored whole-body profile walk cycle — CANDIDATE (2026-09-05)

Source: `art/staging/thad/profile-walk-cycle-01.png` — the one authorized sheet (ledger `thad-profile-walk-cycle-authoring`, attempt 1 of 1, gates PASS; prompt `proofs/thad/prompts/profile-walk-cycle-01.txt`). Six complete right-facing poses on magenta, common baseline. The second half repeated the first with the same leg leading, so poses 4–6 (`unused-poses/`) are not in the cycle; nothing was duplicated and no limb was cut, rotated or transplanted to fabricate the missing contralateral half.

`thad-walk-right/` is exactly what `tools/rig/cut-cycle-sheet.py --poses 1,2,3` wrote and what is installed in `art/actors/thad-walk-right`; the left clip is these frames mirrored whole. `all-six-poses-strip.png` shows every pose beside the stand at Room 5 gameplay scale. Record: `proofs/thad/profile-walk.json` §authoredCycle.
